/**
 * Webhook Events
 * Re-exported from @forgestack/shared for consistency
 */

export { WEBHOOK_EVENTS } from '@forgestack/shared';
export type { WebhookEventType, WebhookPayload } from '@forgestack/shared';
