# =============================================================================
# ForgeStack Web - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# API URL
# -----------------------------------------------------------------------------
# URL of the ForgeStack API
# Local: http://localhost:4000
# Production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL="http://localhost:4000"

# -----------------------------------------------------------------------------
# AUTHENTICATION (better-auth)
# -----------------------------------------------------------------------------
# Secret key for signing auth tokens (must match API's BETTER_AUTH_SECRET)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET="your-secret-key-at-least-32-characters-long"

# Base URL of the web app (used for auth callbacks)
# Local: http://localhost:3000
# Production: https://app.yourdomain.com
BETTER_AUTH_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# PostgreSQL connection string (required for better-auth)
# Must match the API's DATABASE_URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Local: postgresql://postgres:postgres@localhost:5432/forgestack_dev
# Neon: Get from https://console.neon.tech → Your Project → Connection Details
# Supabase: Get from https://app.supabase.com → Project Settings → Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/forgestack_dev"

# =============================================================================
# OPTIONAL: V2 FEATURE CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# STRIPE (for client-side Stripe.js)
# -----------------------------------------------------------------------------
# Publishable key for Stripe.js (starts with pk_test_ or pk_live_)
# Get from: https://dashboard.stripe.com/apikeys
# Note: This is safe to expose in the browser
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"

